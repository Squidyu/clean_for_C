# 🧹 Windows C 盘清理工具  
# BRD – Business Requirement Document  
**版本：V1.0**  
**作者：ChatGPT（产品经理协作版）**  
**日期：2025-12-10**

---

# 1. 产品简介

## 1.1 产品名称
Windows C 盘智能清理工具（Windows C Drive Cleaner）

## 1.2 产品定位
一款面向 Windows 用户的 **安全、透明、清晰、可控** 的 C 盘清理工具，用于解决磁盘占满、系统卡顿、Windows Update 残留文件过多等问题。

## 1.3 产品目标
- 提供清晰的 C 盘空间占用结构  
- 支持模块化清理（可选项 + 展开文件明细）  
- 支持高风险项目的明确警告（如休眠文件 hiberfil.sys）  
- 提供安全清理（避免误删系统关键文件）  
- 提供清理前预测空间、清理后统计  
- 提供日志与可回滚功能（如恢复休眠功能）

---

# 2. 商业目标（Business Goals）

1. **提升用户电脑性能**：解决 C 盘空间不足带来的卡顿、更新失败。  
2. **替代复杂工具**：提供比系统自带“存储感知”更直观、更细节的清理能力。  
3. **增加用户粘性**：作为 PC 管理工具的核心功能之一，为后续增值功能（安全、优化）做铺垫。  
4. **支持售前/运维场景**：为 IT 管理人员提供清晰的空间视图与可控清理。

---

# 3. 用户画像（Target Users）

### 3.1 普通用户
磁盘经常爆红，不懂清理哪些文件是安全的。

### 3.2 重度 PC 使用者
游戏玩家、开发者、设计师，经常产生大量缓存、大文件。

### 3.3 企业 IT 管理人员
需要清理多台电脑并避免误删系统文件。

---

# 4. 用户需求（User Needs）

| 用户痛点 | 工具解决方式 |
|---------|--------------|
| C 盘满但不知道是什么占空间 | 模块化扫描 + 展示各类文件占用 |
| 想手动清理但怕误删系统文件 | 风险等级提示 + 白名单保护 |
| 想要知道清理是否有效 | 显示可清理空间、实际释放空间 |
| 想控制清理内容 | 文件级展开、可勾选清理项 |
| 想深度清理 | 支持休眠文件、Windows 更新残留、大文件查找 |
| 希望可恢复 | 提供日记与恢复休眠功能 |

---

# 5. 产品范围（Scope）

## 5.1 在范围（In-Scope）
- C 盘扫描（模块化）  
- 文件展示（模块级 → 文件级展开）  
- 可选清理  
- 清理进度与失败处理  
- 清理日志  
- 风险提示（休眠文件、高危文件）  
- 支持管理员权限操作（UAC 提升）  

## 5.2 不在范围（Out-of-Scope）
- 实时后台监控  
- 文件备份系统  
- 清理 D/E 盘  
- 云端数据同步  

---

# 6. 功能需求（Functional Requirements）

## 6.1 模块化扫描（核心）

扫描以下模块并输出结构：

| 模块 | 描述 | 风险等级 | 典型路径 |
|------|------|----------|---------|
| 系统垃圾 | Windows 临时文件、预读取文件 | 低 | `C:\Windows\Temp` |
| Windows 更新残留 | 更新缓存、旧版本备份 | 中 | `SoftwareDistribution`、`WinSxS` |
| 浏览器缓存 | Edge/Chrome/Firefox | 低 | 用户目录 |
| 第三方应用缓存 | VSCode、JetBrains、微信等 | 低 | `AppData\Local` |
| 回收站 | 各用户回收站 | 低 | `$Recycle.Bin` |
| 大文件扫描 | File > X MB | 中 | 全盘递归 |
| 应用残留 | 已卸载软件残留目录 | 中 | `Program Files`、`AppData` |
| 休眠文件（hiberfil.sys） | 系统休眠镜像文件 | 高 | `C:\hiberfil.sys` |

扫描结果统一结构（反引号已被转义以避免 markdown 解析）：
```
{
  "module": "系统垃圾",
  "safeLevel": "low",
  "totalSize": 1456000000,
  "files": [
    {
      "path": "C:\\Windows\\Temp\\a.tmp",
      "size": 2400000,
      "lastAccess": "2024-11-10"
    }
  ]
}
```

---

## 6.2 休眠文件（hiberfil.sys）特殊处理

### 功能点
- 检测是否存在  
- 获取大小（通常 = 内存大小）  
- 判断休眠是否启用 (`powercfg /a`)  
- 提供操作：  
  - 禁用休眠并删除 hiberfil.sys（`powercfg -h off`）  
  - 恢复休眠（`powercfg -h on`）  

### 风险提示
- 禁用休眠会导致：  
  - 无法使用休眠  
  - Fast Startup 将被关闭  

用户必须勾选“我已知晓风险”才能执行。

---

## 6.3 文件展示（UX）

- 模块折叠/展开  
- 文件项：路径 / 大小 / 最后访问时间 / 勾选框

---

## 6.4 清理功能

### 6.4.1 清理流程
- 收集用户勾选的文件  
- 按模块删除  
- 异常跳过并记录  

### 6.4.2 清理显示
正在清理系统缓存...
已释放：1.2GB
进度：45%
### 6.4.3 清理结果
- 总释放空间  
- 模块贡献  
- 失败文件  

---

# 6.5 清理日志
- 时间  
- 模块  
- 删除文件  
- 失败项  
- 是否清理 hiberfil.sys  

---

# 7. 业务流程（Flow）
扫描 →
展示 →
选择清理项 →
管理员权限 →
执行 →
展示结果 →
写入日志

---

# 8. 风险分析

| 风险类型 | 描述 | 应对措施 |
|----------|--------|-----------|
| 误删系统文件 | 删除关键文件 | 白名单保护 |
| 无权限 | 操作失败 | 请求管理员权限 |
| 清理 hiberfil.sys 影响体验 | 开机变慢 | 强警告 |
| 扫描耗时 | 对 SSD 较长 | 多线程/可取消 |

---

# 9. 非功能需求（NFR）

- 扫描 ≤ 30 秒（SSD）  
- 实时清理反馈  
- 白名单保护关键系统路径  
- 模块化扩展能力强  

---

# 10. 接口需求（研发用）

- UAC 权限检查  
- `powercfg -h off` → 删除 hiberfil.sys  
- `powercfg -h on` → 恢复休眠  
- `powercfg /a` → 查询休眠可用性  

---

# 11. UI 结构

- 首页 → 扫描按钮  
- 扫描结果 → 模块列表 + hiberfil.sys  
- 清理中 → 进度条  
- 结果页 → 释放空间统计  

---

# 12. 测试用例（UAT）

| 用例 | 预期 |
|------|-------|
| 扫描 | 各模块识别正确 |
| 展开文件 | 正常展示 |
| 勾选逻辑 | 正常 |
| hiberfil.sys 删除 | 成功禁用休眠 |
| hiberfil.sys 恢复 | 成功恢复 |
| 日志记录 | 正常 |

---

# 13. 发布计划

| 阶段 | 时间 | 说明 |
|------|-------|------|
| 需求评审 | T+1 | 完成文档 |
| 原型 | T+3 | 输出 UI |
| 开发 | T+10 | 完成功能 |
| 测试 | T+5 | 验证 |
| 上线 | T+1 | 发布 |

---

# 14. 附录

- hiberfil.sys = 休眠镜像文件  
- Fast Startup = 快速启动  
- UAC = 管理员权限提升  
